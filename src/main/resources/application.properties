spring.application.name=Journal Application

spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=journaldb

#This ensures that MongoDB automatically creates indexes based on the @Indexed annotations in your entity classes. Without this, indexes need to be manually
# created or initialized through scripts
spring.data.mongodb.auto-index-creation=true